/**
 * AudioManager - Audio system manager, extends IManager
 */
define(['../core/imanager'], function(IManager){

	var AudioManager = IManager.extend({

		process: function(){
			
		}
	});

	return AudioManager;
});